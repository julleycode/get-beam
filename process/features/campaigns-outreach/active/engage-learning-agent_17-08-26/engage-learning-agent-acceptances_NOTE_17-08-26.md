---
name: note:engage-learning-agent-acceptances
description: "Formal acceptance record for the program's CONDITIONAL gates — KG-1 accepted by USER 17-08-26; KG-2/3/4 + phase residuals accepted by orchestrator by name"
date: 17-08-26
feature: campaigns-outreach
metadata:
  node_type: memory
  type: note
---

# Engage Learning Agent — Acceptance Record (17-08-26)

## USER acceptance (explicit, in-session message "accept KG-1", 17-08-26 ~23:5x)

- **KG-1 — handle-rename drift: ACCEPTED.** The social blind index derives from a mutable handle; a contact who renames leaves pre-rename rows that later erasure requests cannot match, in BOTH `engage_contact_memory` and `engage_outcomes.contact_bidx`. Accepted as a bounded, documented GDPR residual. Consequences: Phase 2's AC-5 remains scoped to matchable rows; backlog stub for a platform-stable author id remains open as the future remedy; no scope increase into Phase 1.

## Orchestrator acceptances (recorded by name, per validator sign-off requirement)

Phase 2: **KG-2** segment dimension dropped from v1 gate keying (SPEC deviation, no data source exists); **KG-3** LinkedIn social-key erasure out of scope v1 (X-only; phantommm exposes no per-contact outcomes); **KG-4** dashboard track-record render leg = Agent-Probe residual pending the Clerk Playwright auth harness (backend numbers fully gated by F7/F7b/F7c). Plus 2 label CONCERNs closed in cycle 6 (C5-1/C5-2 — verified physically by grep).

Phase 1: **OQ-1** live X metrics/mentions tier + rate limits (needs-live-provider, double opt-in required before any billed probe; mock-first gates are binding, live legs Hybrid); **AC-4 real-path ROI residual** (attribution re-scoped to link-present drafts; site-link-offer work = parked backlog stub with NO home phase; multi-site manual drafts resolve NULL site_id fail-closed); Clerk harness legs; DD-1/DD-2 closed in cycle 7 (grep-verified).

Phase 3a: none needed — **PASS 0/0** (tune-from-observed thresholds live in 3b's config; DISTINCT-contact leg Phase-2-dependent).

Phase 3b: live X platform-delete undo (Hybrid, double opt-in); crisis-lexicon detector QUALITY (Agent-Probe; the ROUTING behavior is Fully-Automated-gated); N=20 / R=0.4 / ceiling=20/hr / dwell=30min are placeholder-conservative and MUST be tuned from observed baselines before any operator enable; rendered-web e2e legs (Clerk harness); C6/C7 closed in cycle 7 (grep-verified).

## Effect on sequencing

With KG-1 accepted, no user gate remains before Phase 2 EXECUTE. Remaining order per /goal: Phase 1 (running, peer session) → 3a → 2 → 3b, sequential — 3a∥2 are parallel-safe on paper but three agents mutating one shared worktree (incl. the peer's P1 EVL runs) makes sequential the safe call.

## Standing operator preconditions (unchanged by these acceptances)

All new flags default OFF; enabling anything in a real environment is a separate explicit operator action; autonomous send additionally requires BOTH kill switches flipped AFTER tuning N/R from observed data. Schema-applied ≠ feature-enabled.
