# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12  # required by FastAPI for multipart/File uploads (blog image upload)

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.3

# Validation & settings
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt
cryptography==44.0.0

# HTTP client
httpx==0.27.2

# Redis & rate limiting
redis==5.1.1
slowapi==0.1.9

# AI
anthropic==0.43.0

# Task queue
celery[redis]==5.4.0
apscheduler==3.10.4

# Enrichment / external
tenacity==9.1.2
resend>=2.0.0

# Payments
stripe>=8.0.0

# Logging
structlog==24.4.0

# Analytics DB (optional, fails gracefully)
clickhouse-connect==0.8.3

# Browser automation (Playwright installed separately via --with-deps)
playwright==1.49.1

# OSINT email→account scanners (manual, behind ENABLE_OSINT_SCAN). NOTE: this is
# the file the Dockerfile installs from — keep OSINT deps here, not only in
# apps/api/requirements.txt, or the prod image won't have them.
user-scanner==1.4.0.1  # email → 103 sites existence + rich extra (username/avatar/joined)
holehe==1.61           # email → 121 sites existence (megadose/holehe)
maigret==0.6.1         # username → 3000+ sites profile URLs (HEAVY: pulls Flask/ipython/lxml/curl_cffi)

# DNS — real MX lookup in email_validator (replaced a fake getaddrinfo:25 check)
dnspython==2.8.0
